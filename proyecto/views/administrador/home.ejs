<%- include('../compartido/estilos-generales')%>
<%- include('nav') %>

<link rel="stylesheet" href="/css/administrador/estilos-home.css">


<div>
    <div class="container" >
        <h1>BIENVENIDO</h1>
        <h2>
            AL SISTEMA DE AGENDACIÓN DE CITAS MEDICAS
        </h2>

        <a href="registrar">
            <button type="button" class="btn btn-info btn-block col-20 mt-5 mb-5">Agregar</button>
        </a>

        <% arregloDoctores.forEach( (objeto)=> { %>
        <ul class="todo-list">

            <div class=items>
                    <li class="lista"><%= objeto.nombre %> </li>
                    <a href="editar/<%= objeto.id %>">
                        <button class="complete-btn">
                            <i class="fas fa-pencil-alt"></i>
                        </button>
                    </a>
                    <a href="">

                    </a>
                    <form action="http://localhost:3001/doctor/eliminarDesdeVista/<%= objeto.id %>"
                          method="POST">
                        <button class="trash-btn" type="submit">
                            <i class="fas fa-trash"></i>
                        </button>

                    </form>

            </div>
        </ul>
        <% }) %>
    </div>

</div>

<script src="/js/alerta.js">
    <% if (parametrosConsulta.mensaje) { %>
    mensajeAlertar(parametrosConsulta.mensaje)
    <% }  %>
</script>
